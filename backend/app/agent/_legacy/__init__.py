"""旧版 Agent 实现（BE-030 四节点闭环）的临时存放区。

为什么存在：一期重写（BE-032~BE-041）期间新旧模块并存，旧文件的
graph.py / nodes.py / prompts.py / state.py 与新设计的同名包冲突
（Python 同目录下包优先于同名模块），故整体搬迁至此保证装配点与
既有测试在切换前持续可用；BE-038 装配切换后本目录连同旧测试一起删除。
"""
